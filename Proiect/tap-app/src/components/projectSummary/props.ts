export type OwnProps = {
    imgUrl: string;
    text: string;
};

export type StateProps = {
};

export type DispatchProps = {
};

export type ProjectSectionSummaryProps = StateProps & DispatchProps & OwnProps;